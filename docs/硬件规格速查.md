# Microduck 硬件规格速查

> **仓库里有四份清单加一份打印说明，各管各的：**
>
> | 文档 | 管什么 | 不管什么 |
> |---|---|---|
> | [BOM](../BOM.md) | **总表**：全部物料、数量（从 MJCF 的 geom 引用数出来的）、成本量级、还没解决的空白 | 具体买哪家、怎么打印 |
> | [电控采购清单](电控采购清单.md) | 电控件**买哪家**：主控 / 摄像头 / ToF / 电源 / 两块 PCB / 线材，淘宝实链 + 避坑 | 数量依据、机械件 |
> | [机械采购清单](机械采购清单.md) | 机械件**买哪家**：轴承 / 紧固件 / 耗材，淘宝实链 | 电控件 |
> | **硬件规格速查**（就是这份） | **参数**：接口、电压、协议、软件侧必须对齐的值 | 价格和店铺 |
> | [print/README](../print/README.md) | **打印件**：哪个零件打几份、文件名规则、打印参数 | 采购 |
>
> 装配用的件号与螺丝对应在 [CAD 仓库](https://github.com/fanhao375/microduck-replica-cad#装配-bom)。


> 📘 新手先看 [硬件入门](硬件入门.md)（一块板一块板讲）；完整推导见 [硬件方案逆向](硬件方案逆向.md)。

> 一页纸版本。完整推导过程和证据见 [硬件方案逆向.md](硬件方案逆向.md)。
> 所有数据来自 `pollen-robotics/microduck` 的源码、设备树和配置文件 —— **代码即规格书**。

**一句话**：Radxa Zero 3W（RK3566）跑 Armbian + 一条 1 Mbps TTL 串口总线挂 15 个
Dynamixel XL330 + 两块自制板。

---

## 系统框图

```
                ┌──────────────────────────────┐
                │   Radxa Zero 3W (RK3566)     │  65 × 30 mm
                │   Armbian · 内核 6.1.115      │  Pi Zero 形态
                └──┬────────┬─────────┬────────┘
      /dev/ttyS2   │        │ 40-pin  │ MIPI CSI
      1 Mbps TTL   │        │  I2C3   │
    ┌──────────────┘        │ 400kHz  └── IMX219 摄像头（转 90°）
    │                       │
    │              ┌────────▼─────────────────┐
    │              │  Pollen RPI Robot HAT    │  65×30mm 自制板(已开源)
    │              │   ├ TLV320AIC3104  0x18  │  音频 codec
    │              │   ├ BMI088   0x19/0x68   │  焊了但没用
    │              │   └ Stemma J5 → ToF 0x29 │  VL53L5CX/L8CX
    │              │  电池经此板供电            │
    │              └──────────────────────────┘
    │
════╪══════════ Dynamixel 总线 · Protocol V2 · 1 Mbps ══════════
    │
    ├── 15 × XL330   ID 左腿20-24 / 颈头嘴30-34 / 右腿10-14
    └── imu_to_dxl v2 板   ID 200   ← LSM6DSV16X
```

**核心设计**：一条总线搞定一切。IMU 不走 I2C，做成 Dynamixel 从机挂在舵机总线上，
和 15 个舵机在**同一次 `sync_read`** 里读出。电池电压也不用电量计，
直接读舵机报告的自身供电电压。

---

## 主控

| 项目 | 值 |
|---|---|
| 模块 | **Radxa Zero 3W** —— 市售模块，**不是定制载板** |
| SoC | **RK3566**（4× Cortex-A55 + Mali-G52 + NPU） |
| 尺寸 | 65 × 30 mm，Pi Zero 形态，40 针树莓派兼容排针 |
| 系统 | **Armbian**（Debian 系），vendor 内核 6.1.115 |
| 策略推理 | ONNX Runtime ≥1.23（装 1.28.0），`dlopen` 加载 |
| NPU 推理 | rknn-toolkit2 —— **Armbian 默认关闭**，需刷 overlay 并重启 |

> 依据：设备树 `compatible = "radxa,zero-3w", "rockchip,rk3566"`；
> 全仓库 "radxa" 出现 182 次。

### 为什么传闻说是树莓派

1. 自制板名字**就叫 "RPI Robot HAT"**
2. Pi Zero 形态 + 40 针树莓派兼容排针
3. 摄像头是**树莓派 Camera v2**（IMX219）
4. **原型阶段确实用的树莓派 Zero 2W** —— `robotd.toml` 注释：50 Hz 控制环
   *"is inherited from the prototype, where it was chosen on a Raspberry Pi Zero 2W"*

前身 Open Duck Mini v2 **至今仍用树莓派 Zero 2W**，因为它不做视觉和 WebRTC 推流。

### 为什么必须 Linux SoC 而非 MCU

要同时跑：ONNX 策略推理 @50Hz、720p30 H.264 硬件编码 + WebRTC 推流、
NPU 目标检测、GStreamer 拥塞控制（占 7.6% 一个核）、蓝牙手柄 + 手机 App + WiFi。
**STM32 / ESP32 做不了。**

---

## 执行器与总线

| 项目 | 值 |
|---|---|
| 电机 | **Dynamixel XL330 × 15**（每个约 18 g） |
| 电气层 | **单线半双工 TTL**（3 线：数据 + VDD + GND），3.3V |
| 协议 | **Dynamixel Protocol V2** |
| 速率 | **1 Mbps**（EEPROM `baud_rate = 3`） |
| 端口 | `/dev/ttyS2` = RK3566 **UART2，M0 pinmux** |
| 挂载设备 | **16 个**：15 舵机 + 1 块 IMU 板 |
| 控制环 | **50 Hz**，一次 `sync_read` 覆盖全部 16 个，寄存器 124–136 |
| Rust 库 | [`rustypot`](https://github.com/pollen-robotics/rustypot) |

> ❗ **不是 RS232，也不是 RS485。** RS485 是差分双线，Dynamixel 的 XM/XH 系列才用。
> XL330 是 TTL 单线半双工。全仓库搜 `rs485|rs232|direction pin` **零命中** ——
> 代码里没有方向控制 GPIO，方向切换由硬件自动方向电路完成（在 HAT 板上）。

### 舵机 ID 分配

```
左腿   20 21 22 23 24    hip_yaw / hip_roll / hip_pitch / knee / ankle
颈头嘴 30 31 32 33 34    neck_pitch / head_pitch / head_yaw / head_roll / mouth
右腿   10 11 12 13 14    （左腿镜像）
IMU 板 200
```

**第 15 个舵机是嘴（ID 34，索引 9）。** 所有策略都是 `obs[1,61] → act[1,14]`，
动作向量**跳过这个索引** —— 嘴由上层逻辑控制，不由策略控制。

### 总线可靠性（官方实测）

| 指标 | 值 | 出处 |
|---|---|---|
| 丢包率 | **约 8 次/分钟 ≈ 0.27%** | *"Measured on a bench robot at ~8 drops a minute"* |
| 官方定性 | *"One dropped Dynamixel transaction is **ordinary**"* | `robotd/src/main.rs` |
| 症状 | 随机小抽搐（丢读导致策略停一拍回 hold 姿态） | 同上 |
| 容错 | `COAST_TICKS = 3` 用上个好样本滑行 60ms | 同上 |
| 容忍上限 | `max_consecutive_errors = 10` | `robotd.toml` |
| 循环稳定性 | `missed=3` / 15022 ticks，保持 50.0 Hz | `roadmap.md` |
| IMU 数据变质 | 有专门的 `StaleImuTracker`，注释写 *"Known to happen"* | `bus.rs` |

总线利用率估算 **约 25%**（约 500 字节/tick ÷ 1 Mbps ÷ 20ms）——
**瓶颈不是带宽，是信号完整性和单点故障**：单端 3.3V 信号、15 个舵机菊花链、
线束穿过动态弯折的颈部、旁边就是电机 PWM 噪声源。

---

## 两块 Pollen 自研板（一块已开源、一块需自绘）

### ① `imu_to_dxl` v2 —— 元件极少，最容易自己做

| 项目 | 值 |
|---|---|
| IMU 芯片 | **LSM6DSV16X**（ST 6 轴，带 SFLP 硬件姿态融合块） |
| 接口 | **Dynamixel 总线**（不是 I2C） |
| 总线 ID | **200** |
| 寄存器地址 | **124** |
| 控制环读取 | 12 字节 |
| 完整诊断块 | 20 字节（另含原始加速度、采样计数、状态标志） |

**12 字节块布局**

| 字节 | 内容 |
|---|---|
| `0..6` | gyro x/y/z，`i16` 小端，±500 dps，**17.5 mdps/LSB** |
| `6..12` | SFLP 四元数 x/y/z，**IEEE fp16**；`w = √(1 − x² − y² − z²)` |

**自己做需要**：LSM6DSV16X + 一颗小 MCU 做 Dynamixel V2 从机
（STM32G0/G4、CH32V203、ESP32 均可）+ 半双工 TTL 收发器，供电从总线取。

### ② Pollen RPI Robot HAT —— 官方已开源，直接打样

**65.0 × 30.9 mm，板厚 1.0 mm，4 层板**（取自官方 KiCad 与 Gerber，见 [BOM](../BOM.md)）。STL 网格量出的 65.02 × 30.02 × 0.84 是仿真近似值，**下单不要用**。插 40 针排针。

| 器件 | I2C 地址 | 说明 |
|---|---|---|
| **TLV320AIC3104** | `0x18` | TI 音频 codec，I2S 数据 + I2C 控制 |
| **BMI088** | `0x19` / `0x68` | **焊了但没用**（注释原文 "dormant"） |
| **ToF** | `0x29` | 不在板上，经 **Stemma J5** 外接 |

- I2C：40 针 **pin 3 / pin 5**（`GPIO1_A0`=SDA，`GPIO1_A1`=SCL），**400 kHz**
- 上拉：**单对 10k，R12/R13**
- 音频时钟：MCLK **12 MHz**，I2S sysclk **12.288 MHz**（256×48kHz）
- 麦克风接 **Mic3R**（单声道右 PGA），其余输入全关
- **机器人内部供电经此板**

---

## 传感器

| 类型 | 型号 | 接口 | 备注 |
|---|---|---|---|
| IMU | **LSM6DSV16X** | Dynamixel 总线 ID 200 | 芯片内 SFLP 出四元数，主机不跑融合 |
| ToF | **VL53L5CX / VL53L8CX** | I2C `0x29` @ i2c3 | 15 Hz，多区测距 |
| 摄像头 | **IMX219**（树莓派 Camera v2） | MIPI CSI，I2C `0x10` | **转 90°**，见 [硬件方案逆向](硬件方案逆向.md#七传感器) |

- 摄像头传感器钉在 1920×1080@30 模式（避开开机默认模式的 21 fps 上限），
  ISP 缩放输出，默认 720p30 @ 2 Mb/s
- 编码走 **Rockchip MPP 硬件编码器** → WebRTC
- HFOV 约 62°

---

## 电源与电池

| 项目 | 值 |
|---|---|
| 电池 | **索尼 NP-F550**（L 系列摄像机电池）。⚠️ 上游网格名 `np_f970` 有误导，实测尺寸是 F550 |
| 配置 | **2S 锂离子**，标称 7.2 V |
| 满电（带载） | **8.2 V** |
| 空（带载） | **6.6 V** |
| 电量计 | **无** |
| ADC | **无** |

> 注释原文：*"There is no fuel gauge and no ADC. The only measurement available is
> what the servos report as their own supply."*

**电压直接通过 Dynamixel 协议读舵机报告的自身供电电压** —— 即"从总线上看到的电池"，
会随负载下沉、静止时回升。所以 6.6–8.2 V 是**带载可用区间**，不是电芯化学量程。

电池 EMA（约 10 秒时间常数）跌到 6.6 V → **自动优雅坐下并关机**。

> 💡 复刻友好：**不需要设计任何电池监测电路。**

---

## 全部接口一览

| 接口 | 用途 | 参数 |
|---|---|---|
| **UART2** (`ttyS2`) | 15 舵机 + IMU 板 | 1 Mbps，TTL 半双工 |
| **I2C3** (pin 3/5，M0) | 音频 codec / ToF /（未用的 BMI088） | 400 kHz |
| **I2S3** | 音频数据 | MCLK 12 MHz，sysclk 12.288 MHz |
| **MIPI CSI** | IMX219 摄像头 | I2C 控制 `0x10` |
| 蓝牙 | 手柄（`padd`）、手机 App（`btd`） | 板载 |
| WiFi | WebRTC 推流、配网（`configd`） | 板载 |
| USB-C | 供电 + maskrom 刷机 | **PD 协商被 overlay 牺牲** |

---

## 软件侧必须对齐的参数

| 参数 | 值 | 说明 |
|---|---|---|
| 控制环 | **50 Hz** | 继承自树莓派原型，**在 Radxa 上从未重新验证** |
| 策略接口 | `obs[1,61] → act[1,14]` | 加载时校验，51 维旧版被拒绝 |
| `action_scale` | 0.9（走）/ 0.8（轮滑） | |
| 位置 P 增益 | **200** | 站立时 ×0.8 |
| 动作低通 | head **0.5** / legs **0.7** | **必须与训练一致**，否则迁移退化 |
| 电压自适应 | 默认关 | `scale × (7.4 / 实测EMA)`，钳位 6.0–9.5 V |

---

## 复刻清单：能买的 vs 要自己做的

| 部件 | 状态 | 备注 |
|---|---|---|
| Radxa Zero 3W | ✅ 直接买 | 市售模块 |
| Dynamixel XL330 × 15 | ✅ 直接买 | **成本大头，$359–€629**（渠道差价极大，见 [BOM](../BOM.md)） |
| **NP-F550** 电池 + 卡座 | ✅ 直接买 | 通用摄像机配件。**别买 F970** |
| 树莓派 Camera v2（IMX219） | ✅ 直接买 | |
| VL53L8CX 模块 | ✅ 直接买 | Stemma/Qwiic 现货 |
| **`imu_to_dxl` 板** | 🔧 **自己画** | 协议已完整还原，元件仅 3 样 |
| **HAT 板** | ✅ 下载官方 Gerber 打样 | 4 层板。不要录音喇叭可整块省略，但**半双工方向电路必须另配转接板**，见 [电控采购清单](电控采购清单.md#六两块-pcb) |
| 控制软件 | ✅ 官方 Apache-2.0 | 主控同款则**可直接跑** |
| 策略 | ✅ 官方 9 个 ONNX | 硬件不变即可用 |

> 💰 **成本提醒**：光 15 个 XL330 就要 **$359–€629**（ROBOTIS 国际站 $23.90 起，欧洲含税 €41.95 封顶），
> 已远超整机 **$399** 售价。$399 是规模化价格。

---

## 三个必踩的坑

1. **Armbian 默认在 UART2 上跑登录控制台** —— `serial-getty@ttyS2` 的 `agetty`
   会占着串口。必须 `systemctl mask serial-getty@ttyS2`。
   官方是用 `fuser -v /dev/ttyS2` 查出来的。

2. **i2c3 与 FUSB302 抢引脚** —— RK3566 的 i2c3 在原厂 DTB 里跑 M1 pinmux
   服务 USB-C PD 控制器；用 pin 3/5 的 M0 就得禁用 fusb302，**失去 PD 协商**。
   好在 FUSB302 上电默认 CC 呈 Rd，普通 5V 充电仍然可用。

3. **NPU 默认关闭** —— Armbian 出厂不开，跑 rknn 模型要刷 overlay 并重启。

---

## 结论

**这套硬件没有不可复刻的部分。** 主控是市售模块；两块 Pollen 自研板里，
**HAT 板官方已完整开源**（KiCad + Gerber + BOM + 贴片坐标，下载即可打样），
真正需要自己画的只有 `imu_to_dxl` 一块 —— 而它元件极少、协议已完全还原。

真正的门槛是**成本**（15 个 XL330 比整机售价还贵），不是技术。

> 数据来源：`duck-control/src/{model,imu,bus}.rs`、`robotd/src/main.rs`、
> `deploy/robotd.toml`、`deploy/audio/*.dts`、`deploy/overlays/*.dts`、
> `tof/src/*.rs`、`mediad/src/*.rs`、`docs/design/robotd-design.md`、
> `docs/project/{media-bringup,roadmap}.md`

---

## 官方规格 vs 本仓库逆向（交叉验证）

官方于 2026-08-27 发布，公布了部分规格。下表把**官方公布**与**本仓库从源码逆向所得**
逐项对照 —— 两边互相印证，也各自补足了对方没有的信息。

| 项目 | 官方公布 | 本仓库逆向所得 | 结论 |
|---|---|---|---|
| SoC | RK3566，四核 A55 @1.8GHz，Mali-G52，NPU **0.8 TOPS** | RK3566（设备树 `compatible`） | ✅ 一致 |
| **主控模块** | **未公布** | **Radxa Zero 3W** | 🔍 **逆向独有** |
| RAM / 存储 | **1 GB / 32 GB eMMC** | 源码中无佐证 | 🆕 **官方独有**：正是 Radxa Zero 3W 的一个 SKU。<br>⚠️ 复刻建议 2G/16G，见 [电控采购清单](电控采购清单.md#该买哪个配置) |
| 电池 | **NP-F550，2600 mAh，续航约 1 小时** | NP-F550，2S，8.2 V 满 / 6.6 V 空 | ✅ 一致 |
| ToF | **8 × 8** LiDAR | VL53L5CX / VL53L8CX @ `0x29` | ✅ 该系列正是 8×8 |
| 摄像头 | 前置摄像头带指示灯 | **IMX219**（Pi Cam v2），I2C `0x10`，**转 90°** | 🔍 逆向更细 |
| **IMU** | **2 个（身体 + 头部）** | LSM6DSV16X **在用**；BMI088 源码标注 **dormant / unused** | ⚠️ **矛盾，见下** |
| 电机 | 15 DOF，铰接可抓取的喙 | 15 × Dynamixel XL330，含完整 ID 表 | ✅ 一致 |
| 尺寸重量 | 高 25 cm，宽 **14 cm**，< 800 g | 实测 **144 × 141 × 264 mm**，**737.2 g** | ✅ 一致 |
| **NFC** | **双天线** | 源码中**无任何提及** | 🆕 **官方独有** |
| 音频 | 麦克风 + 喇叭 | TLV320AIC3104 @ `0x18`，麦克风走 Mic3R | 🔍 逆向更细 |
| **主总线** | **未公布** | TTL 半双工 1 Mbps，`/dev/ttyS2`，Protocol V2 | 🔍 **逆向独有** |
| 连接 | Wi-Fi、蓝牙 | 同上（Radxa 板载） | ✅ 一致 |

### ⚠️ 那个矛盾：官方说 2 个 IMU，软件只用 1 个

官方规格写 **"2× IMUs (body and head)"**，但源码里：

- **在用的只有 LSM6DSV16X 一个** —— `imu.rs` 开头就写 *"One IMU, one code path"*
- HAT 板上的 **BMI088** 在设备树注释里被标为 **"dormant"**、
  **"(unused but still connected)"**

**结论：第二个 IMU 物理上焊在板上，但出厂软件不使用它。** 复刻时可以直接省略。

---

## 开源状态：官方立场（2026-08 更新）

> CNX Software 报道原文：
> *"While the Microduck's software stack is open source, nothing was said about the
> mechanical and hardware design files, and **Pollen Robotics told the press not to
> refer to the robot as 'open-source hardware' (for now)**."*

**这条要小心解读。** 官方拒绝的是把整机称作「开源硬件」这个**标签**，
不等于一块板都没公开 —— 事实上 **RPI Robot HAT 板是完整开源的**
（[`elec_RPI_Robot_HAT`](https://github.com/pollen-robotics/elec_RPI_Robot_HAT)，Apache-2.0，
含 KiCad 工程、Gerber、BOM、贴片坐标）。真正没公开的是 `imu_to_dxl` 板、
可编辑机械 CAD、整机 BOM 与装配文档。

那个 **"(for now)"** 留了口子，没给时间表。

> 勘误 2026-09-03：本文此前写「硬件不开源是官方主动划的线」，表述过强，已修正。

### 外部验证：有人独立得出了相同的主控结论

2026-08-31，X 上 [@tspy](https://x.com/tspy/status/2094249218735300630) 发布了一份
「Microduck 物理架构拆解」（169 赞），独立列出：

> 执行机构 15 × Dynamixel XL330 · 主控 **Radxa ZERO 3W / Rockchip RK3566** ·
> 1 GB RAM + 32 GB · 双 6 轴 IMU · 8×8 ToF · 前向摄像头 · 2× NFC 天线 ·
> 电池 NP-F550 · 控制频率 50 Hz

**与本仓库的逆向结论完全一致**，包括最关键的 **Radxa Zero 3W** 这一条 ——
两条独立路径得出同一答案，可信度大幅提升。

> 📌 **注意：截至 2026-08-31 市面上没有任何实物**（圣诞前才发货）。
> 所以目前所有「teardown」都是**基于公开软件与规格的推理**，不是物理拆解。
> 本仓库的深度（I2C 地址、寄存器布局、12 字节数据块、总线协议）
> 超出规格清单一层，来源是把 Rust 源码当规格书读。

同一串讨论里，@tspy 被问到成本时的回答也与本仓库一致：**「成本貌似比直接购买要高」**。

### 社区正在追问的三个 issue（截至 2026-08-31 官方均未回复）

| Issue | 诉求 |
|---|---|
| [#175](https://github.com/pollen-robotics/microduck/issues/175) | 要 **STEP 文件** —— STL 没有法线信息，曲面渲染成多边形块 |
| [#173](https://github.com/pollen-robotics/microduck/issues/173) | 要 **3D 打印源文件** |
| [#174](https://github.com/pollen-robotics/microduck/issues/174) | 问电池能否原位充电，并直接问**电源板原理图是否在开源计划内** |

查了近 10 个 issue，**Pollen 成员一条都没回**，但仓库仍在每日更新。

> 这也说明本仓库做的事情有价值：社区里目前没有人从 MJCF 反推装配图，
> 也没有人从 Rust 源码系统性地扒出电控方案。
