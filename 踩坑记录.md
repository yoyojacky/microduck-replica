# 踩坑记录

复刻过程中真踩过的坑，**每条三句：现象、原因、怎么办**。按阶段分，上手前扫一遍。

分析和证据在别的文档里，这里只记结论。有新坑往对应阶段里加，写直白点。

> 时间顺序的构建过程在 [构建日志](构建日志.md)。

---

## 买东西

**电池别买 NP-F970，是 NP-F550**
上游网格文件名叫 `np_f970.stl`，但量出来是 39 × 21 × 71 mm，这是 F550 的尺寸。真 F970 厚约 60 mm，**装不进去**，而且 300 g 会吃掉整机预算的三分之一。
→ 买 NP-F550（2S，6.6–8.4 V）。详见 [BOM](BOM.md)。

**飞特 HD-1910 的线是 2.0 mm，飞特调试板的口不是 —— 配 ID 得飞线**
HD-1910 原装线是 AMP2.0-3P（2.0 mm 间距）。FE-URT-2 调试板的 TTL 口比这个大，**插不上**，板子也没给 2.0 的口。
→ 当天就要配 ID 的话，**剪一根舵机线**（15 根牺牲一根），三根裸线这么接：

```
舵机线                  FE-URT-2
脚 1  Signal    →   S    焊到板背面白口 S 脚的焊点上（临时也可镀锡后直接插进 S 孔）
脚 2  Vcc       →   V1   蓝色接线端子
脚 3  GND       →   G    蓝色接线端子
```

**中间那根一定是 V**（飞特和 Dynamixel 在这点上一致），S 和 G 接反了不会烧、只是通不了，对调即可。
**V 接错才会烧。** 配 ID 空载，USB 5 V 够用（HD-1910 从 4 V 起工作）—— 先用万用表量蓝色端子 G–V1 有没有 5 V，没有就拨那个开关。
→ 正式的做法是买「PH2.0 3P 端子线」+ 板子那种规格的端子线各几根，对焊成转接线。买前翻翻舵机包装盒，有时附一根。
→ 这条值得直接告诉飞特：正式版是否附转接线。

**飞特脚序跟 Dynamixel 完全相反**
飞特 HD-1910：`1=Signal 2=Vcc 3=GND`。Dynamixel XL330：`1=GND 2=Vcc 3=Signal`。
→ 好在两种间距不同（2.0 vs 2.5 mm）插不进对方。只剩「自己压线时压反」这一种可能 —— 压完先用万用表点一遍。

**别拿 HL-2915 当参考**
飞特 HL-2915-C002 是 9–14 V 舵机，2S 电池（最高 8.4 V）**带不动它**，而且它的脚序又跟 HD-1910 相反。
→ 本方案只认 HD-1910-C001。

**电池满电 8.4 V 正好等于 HD-1910 的电压上限**
HD-1910 工作范围 4–8.4 V。NP-F550 满电 8.4 V，**零余量**。带载压降到 6.6–8.2 V 才回到安全区。
→ 刚充满别直接堵转测试。长期存放按 3.8 V/cell 存。装机前把舵机的过压保护打开。

**Radxa Zero 3W 低配版是裸板**
2 GB 无 eMMC 版本**没焊排针、没有存储**，只能 microSD 启动。
→ 自己买 2×20 排针（2.54 mm，立式，买彩色的好数脚）和**高耐久** microSD（SanDisk High Endurance / 海康 PLUS，32 或 64 GB，买两张一张做备份）。

**URT-2 的 V1 端子别灌外接电源**
板上拨码是 TTL 电平选择（3.3 / 5 V），**不是**供电切换。USB 插上 V1 就有 5 V，跟 USB 怎么连的不清楚，可能直通。往 V1 灌 7.4 V 有反灌进电脑或 Radxa 的风险。
→ 外接电源直接给舵机的 V 和 G，URT-2 的 G 一起接负极共地，V1 空着。URT-2 只管信号。

**飞特舵机出厂 ID 全是 1**
15 颗一起接上去，全都应答，分不出谁是谁。
→ **设 ID 时一次只接一颗。** 改完贴标签（写关节名 + ID），拔掉，换下一颗。步骤和 ID 表见 [调试记录 · 舵机配置](调试记录.md#舵机配置--拿到货一步一步做)。

---

**ToF 模块：L8CX 那块板装不进脸壳，L7CX 固件又不认**
现象：先买了 VL53L8CX，国内常见那块板 **33.4 × 10.2 mm**，脸壳（`face_part`）装不下；
为了短，改买 L7CX（26 mm），结果固件启动就报错退出。两块都白买。
原因：L7CX 是 90° 宽视场的**另一颗芯片**，revision ID 对不上。官方 `tof` 固件只认 `0x02`（L5CX）
和 `0x0C`（L8CX）两个，别的直接报 `neither a VL53L5CX nor a VL53L8CX` 然后退出 ——
固件里只编了这两个 ULD 驱动，不是改个 ID 能绕过去的。
→ 买 **VL53L5CX（VL53L5X V2 模块）**：固件认，**孔距 20 mm 正好对上打印件**（同事实测打印件孔位 20 mm，
L8CX 那块板是 28 mm，根本装不上），板子也最小（**15.8 × 10 mm** 板体 / 24.7 mm 含安装耳），
还便宜 25 块。**选型顺序是先看芯片固件认不认（只有 L5CX / L8CX 两颗），再看板子塞不塞得进**——
反过来先挑尺寸就是这次的结果。L8CX 这颗芯片本身没问题，是那块板子太长；
要是能找到短板型的 L8CX 也能用，孔距和镜头位置按实物量。
三种板型尺寸对比见[电控采购清单](docs/电控采购清单.md#四tof-测距--vl53l5cx-或-vl53l8cx8×8-多区)。

## 画板 / 打板

**立创下单页的「替换」不回写工程**
下单页点「替换」换料号，只对那一单生效。EasyEDA 工程里的元件还是旧料号，**下次导 BOM 又变回去**，再报一次「私有库 0」。
→ 在 EasyEDA 工程里把元件换掉（删了重放同坐标），不要只在下单页换。

**有源晶振和无源晶体，3225 四脚长得一模一样**
无源晶体的 2 脚和 4 脚是同一片金属盖、内部短接。本板 X1 按有源画（2=GND，4=VDD），贴上无源的**一上电就是 3.3 V 对地短路**。立创搜「3225」出来大部分是无源，`C13738`（基础库免换料费）最容易顺手点错。
→ 只认标签写「有源晶振 / OSC」的。本板用 `C5917307`。

**星通时频 C387369 立创贴片备料库没有**
下单时被归到「使用私有库存的元器件」，私有库 0。
→ 换 `C5917307`（雅晶鑫，5 mA，同封装）。

**加了器件后 Gerber / BOM / 坐标全要重导**
改了原理图（比如加 J4/J5）后，三个文件都变了。用旧 Gerber 打板，板子上没那两个座子的孔。
→ 每次改完导出全套，文件名带时间戳，认最新的那份。

**J4/J5 选的 PH2.0 座子，间距对了，外壳跟飞特线的插头不配，插不进去**
飞特 HD-1910 的线是 AMP2.0-3P 插头，2.0 mm 间距没错，但插头外形和 JST PH 的座子外壳（`B3B-PH-K-S`）对不上。评审时就有人提醒「板端座子没实物验证」，果然。
→ 首板：把座子外壳削掉，光留针，能插能用。v2：拿飞特线实物去配座子（问飞特要板端座子型号，或买几种 2.0 mm 3P 座子试插），**投板前实物插一次**。

**GitHub Release 附件名里的中文会被清掉**
上传 `SolidWorks图纸.zip`，存成了 `SolidWorks.zip`。README 里写的下载链接对不上。
→ Release 附件直接用英文名。

---

## 飞线（不打 HAT 时）

> 完整接法在 [不打 HAT](docs/不打HAT.md)，这里只列会咬人的。

**转接板的逻辑供电只能取 3.3 V**
半双工转接板 MCU 侧有个 VCC。取 5 V 的话它的 TX 会用 5 V 电平打进主控 3.3 V 的 RX 脚。
→ 从 40 针 pin 1（3.3 V）取。ToF 从 pin 17 取，两个不抢。

**5 V 灌进 40 针是无保护的**
从 pin 2/4 喂 5 V 绕过了板子自己的 USB 输入保护。**接反当场烧板。**
→ 插之前先量降压模块输出的极性和电压。

**舵机主干别用杜邦线**
15 颗峰值好几安培，杜邦线 24–28 AWG 只扛 1 A 左右，会发烫甚至熔皮。
→ 主干用 18–20 AWG 硅胶线。

**电池到舵机那一路要串保险丝和开关**
锂电池短路会喷火。官方 HAT 那条路上有热敏 + 「3A max」标注，飞线方案裸接就什么都没有。
→ 串 5 A 保险丝 + 电源开关。

**没有关机检测信号**
HAT 上的 LM5050-1 给主控一个关机信号，飞线方案没有。每次断电都是硬拔，**最伤 microSD**。
→ 先 `make shutdown` 或手柄关机手势，等系统关干净再断电。


---

## 舵机 / 校准

**飞特 FD 调试软件：先装驱动、用最新版、波特率选 1 Mbps**
现象三连：插上 URT-2 一颗舵机都扫不到；装完驱动能连了，但舵机型号读出来是 `unknown`；或者能连就是扫不出来。
原因：① URT-2 的 USB 转串口芯片要装驱动，设备管理器里没有 COM 口就是没装；② FD 版本旧，型号表里没有 HD-1910 这种定制型号，认不出就显示 unknown，**但寄存器读写完全正常**；③ HD-1910 出厂波特率是 1 Mbps（寄存器 6 = 0），FD 里波特率不选 1000000 就扫不到。
→ 顺序：装驱动 → 设备管理器确认有 COM 口 → FD 里波特率选 1000000 → 用[飞特官网](http://doc.feetech.cn/)最新版 FD。型号显示 unknown 不用管，别以为舵机坏了。

**HD-1910 的位置计数方向跟官方 XL330 相反，15 颗全反**
现象：拖滑块时 3D 模型跟实际反着动；按官方站姿发角度，腿往反方向弯。
原因：HD-1910 读数增大的方向跟 XL330 相反，而机械装法是照搬官方的，所以每个关节都整体反了一次 —— 是全体反，不是个别几颗装反了。
→ 15 颗方向全取 −1：[`tools/servo-web/directions.json`](tools/servo-web/directions.json)，robotd 里是 `[bus.feetech] directions`。网页上每颗旁边的 ± 按钮逐颗核对过。

**HD-1910 不认「扭矩开关写 128」这种中位校准**
现象：内存表里明写着「写 128：任意当前位置较正为 2048」，照着做舵机回「成功」，但偏移寄存器（31）一个都没变。代码要是接着把目标位置写成 2048，关节反而被拉回旧零位。2026-09-20 实测 4 轮 15 颗，解锁、扭矩关着也一样。
原因：固件 3.46 不支持这条。协议手册 4.9 的支持表里写着：SMS ≤2.54 和 STS ≥3.10 支持写 128，**HLS ≥3.43 不支持**，只支持 0x0B —— HD-1910 的表现跟 HLS 那一行一致。
→ 用 **0x0B 位置校准指令**（不带参数 = 校成 2048，带参数 = 校成指定读数）。校完一定回读位置和偏移确认，别信应答里的「成功」：飞特很多不支持的操作照样回成功。

**校准时别关着扭矩等**
现象：关扭矩后等几十毫秒再读位置，颈部（30）读数掉了 80 步（7°），校出来的零位是歪的。
原因：扭矩一关关节就被重力压下去，等得越久掉得越多。
→ 关扭矩到校准完成中间不要 sleep，几毫秒就够；校准后立刻把目标位置写成新读数再开扭矩。

---

## 软件

**Armbian 出厂 `overlay_prefix=rk35xx`，RK3566 的 overlay 却叫 `rk3568-*`**
前缀不对，加载器什么都找不到、静默失败，板子正常启动，**但没有 `/dev/ttyS2`**。你会以为是线接错了。`apt upgrade` 换内核后可能打回原形。
→ `/boot/armbianEnv.txt` 里改成 `overlay_prefix=rk3568`，加 `overlays=uart2-m0`，重启。官方 `setup-board.sh` 就干这个。

**内核 console 默认也在 ttyS2 上**
UART2 是 RK3566 的调试串口，Armbian 把内核日志往那打 —— 也就是往舵机总线上打。
→ `/boot/armbianEnv.txt` 的 `extraargs` 里去掉 `console=ttyS2,1500000`。

**Armbian 默认在 UART2 上跑登录控制台**
`agetty` 占着 `/dev/ttyS2`，舵机怎么都不通。线接对了也不通，八成是它。
→ `systemctl mask serial-getty@ttyS2`，然后 `fuser -v /dev/ttyS2` 确认没别的进程。

**别关 armbian-zram-config**
Armbian 默认把日志放 zram（内存）里不落盘，是在保护 SD 卡。有人为了看日志方便把它关了，写入量翻几倍。
→ 留着。官方 `deploy/README.md` 原话：不落盘是「有意的安排」，关掉是「每块板自己的决定，不是默认」。

**i2c3 用 pin 3/5 会失去 USB-C PD 协商**
RK3566 的 i2c3 原厂跑在 M1 pinmux 服务 FUSB302（USB-C PD）。官方 overlay 把它 mux 到 M0（pin 3/5）并禁用 fusb302。
→ 接受这个代价：USB-C 仍给 5 V，只是不协商。机器人供电走电池，跟这无关。

---

**`wsl --mount` 挂不上 USB 读卡器，别指望它往烧好的卡里写文件**
想用 WSL2 把 Armbian 的首次开机预设 `.not_logged_in_yet` 写进卡的 ext4 根分区，`wsl --mount \\.\PHYSICALDRIVE2 --partition 1` 直接报 `Wsl/Service/AttachDisk/MountDisk/HCS/ERROR_FILE_NOT_FOUND`。这是 WSL 对 USB 可移动盘的限制，跟卡烧没烧好无关。
→ 反过来做：先把文件塞进镜像再烧。WSL 里解压 `.img.xz` 到本地盘（不能放 `/mnt/` 下，loop 挂不了），`mount -o loop,offset=$((32768*512))` 挂根分区写文件，再烧生成的 `.img`。脚本在 [`tools/radxa/`](tools/radxa/)。

**Armbian 官方镜像在 Zero 3W V1.12（AIC8800 WiFi 版）上起不来：灯双闪、永远进不了系统**
2024-11 以后出厂的 Zero 3W 是 V1.12，Armbian 26.2.1 / 26.8.1 的 vendor 镜像烧上去，灯有心跳双闪（内核起了），但根本进不了 initramfs，系统盘一个字节都没被碰过，局域网里永远找不到。
瑞莎官方镜像同一张卡同一块板一次就起。逐项排除（卡、SDR104 高速模式、供电、设备树全一样）后，差异只剩引导程序：
Armbian 给 zero3 配的是主线 U-Boot 2025.10 + 2024-01 的内存初始化固件 v1.21；瑞莎自家是 rk U-Boot 2017.09 + DDR v1.23（2024-09）+ BL31 v1.45。
→ 把瑞莎 `u-boot-rk2410` 包里的 `idbloader.img` / `u-boot.itb` 写到卡的扇区 64 / 16384，再给 Armbian 写一个 `/boot/extlinux/extlinux.conf`（瑞莎 U-Boot 走 extlinux，不读 boot.scr），内核和系统一字不改，立刻起来。
脚本在 [`tools/radxa/`](tools/radxa/)，一键做卡。没串口也能定位这种问题的办法：读卡看根分区有没有被写过、在 initramfs 里塞 LED 信号和黑匣子日志。

**V1.12J（2026-09 这批）光换引导还不够，设备树也得换**
现象：按前一条换了瑞莎引导，Armbian 还是起不来；瑞莎官方 B1 镜像能起。有人还碰到冷启动插着 HDMI 就不开机。
原因：这批板子的 WiFi 换成了 AIC8800DS2，Armbian 自带的 `rk3566-radxa-zero3.dtb` 不认；瑞莎 B1 镜像里那份认。
→ 引导和 **DTB 都从 B1 镜像里拿**，extlinux 的 `fdt` 指向拷过来的那份，内核和根文件系统留 Armbian 的
（两边都是 6.1 vendor 内核，能混）。群友 2026-09-20 实测跑通。换 DTB 后记得验 `/dev/ttyS2` 和 USB 串口还在不在，
overlay 是 Armbian 的，节点不一定对得上。命令见[镜像使用说明](tools/radxa/镜像使用说明.md#6-如果你的板子不是这一批)。

**AIC8800 不支持 GCMP-256，路由器开了 WPA3 混合模式就连不上**
Zero 3W V1.12 的 AIC8800D80/DS2 只有 CCMP-128。家用路由器（TP-LINK 之类）默认 WPA2/WPA3 混合，WPA3 那半用 GCMP-256，握手失败。
→ wpa_supplicant 里强制 `key_mgmt=WPA-PSK`、`pairwise=CCMP`、`ieee80211w=0`（做卡脚本已带），或者路由器改成纯 WPA2。5 GHz 和 2.4 GHz 都能连。

**板子插电脑没有 USB 串口：要 `dwc3-peripheral` overlay**
Zero 3W 的 OTG 口设备树写的是 `dr_mode = otg`，Armbian vendor 内核下插电脑不会自动切成设备模式，`g_serial` 加载了电脑也看不见。
→ extlinux 里挂 `rk3568-dwc3-peripheral.dtbo`（Armbian 自带），再 `g_serial` + `serial-getty@ttyGS0`，插电脑就多一个 COM 口，root 直接登，救急神器。做卡脚本已带。

**板子上跑官方脚本，GitHub 下不动**
`setup-board.sh` / `install.sh` 都从 raw.githubusercontent.com、api.github.com、objects.githubusercontent.com 下东西，国内直连基本卡死（IPv6 优先更糟）。
→ 电脑上的 Clash 开「允许局域网」，板子 `/etc/environment` 写 `http_proxy=http://电脑IP:7890`（做卡脚本 `card.conf` 里 `HTTP_PROXY=` 填上就行）。apt 走 Debian 源不用代理。

**Armbian 首次开机预设少两个变量会停在提示符上等人敲键盘**
`/root/.not_logged_in_yet` 里不写 `PRESET_DEFAULT_REALNAME` 会停在「Please provide your real name」，不写 `PRESET_CONNECT_WIRELESS` 且没网会停在「Connect via wireless? [Y/n]」。无头板子就此卡住，密码永远是出厂的 root / 1234。
→ 两个都写上（做卡脚本已带）。

## 工具

**URT-2 先给舵机上电、后插 USB，总线就通不了**
现象：15 颗全不应答，只有广播 ping 能收到几个全 0 的字节；万用表量总线 S–G 空闲只有 2.1 V（正常 4.5 V 上下）；不插 USB 时板子 5 V 排针上莫名有 2.1 V。板子拔掉舵机空载 S–G 又是好的 4.5 V。原因是板子逻辑电只来自 USB，舵机那边先把总线拉到 5 V、板子还没电，电流从 S 脚倒灌进板子的 5 V 轨，等 USB 再插上时电平转换那部分已经卡在一个半死状态，把总线压在 2.1 V。
→ **顺序：先插 USB，再给舵机上电**。已经反了就把两边都断开重来。别急着怀疑 S/G 插反或板子坏了（2026-09-17 在这上面耗了一小时）。

**VS Code 的 Markdown Editor 扩展不显示 HTML 图片**
README 用 `<table><img>` 拼的两栏卡片，那个扩展不解析裸 HTML 里的相对路径，图全空。GitHub 上是好的。
→ 用 VS Code 自带预览（`Ctrl+Shift+V`）。

**Windows 上 `core.autocrlf=true` 会毁掉二进制文件**
PDF 头部多为 ASCII，git 自动探测误判成文本，checkout 时做 LF→CRLF 转换，文件直接损坏。
→ `.gitattributes` 里把 `*.pdf *.stl *.step *.png` 等全部声明 `binary`。
